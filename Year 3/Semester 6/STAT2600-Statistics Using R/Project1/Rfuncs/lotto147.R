lotto147 - function(){
#
#   Returns the lotto consisting of 5 numbers from
#   the numbers 1 through 47.
#
     numbs - 147
     selected - sort(sample(numbs,5,replace=FALSE))
     return(selected)
}
